import { AsnOneDirectiveDirective } from './asn-one-directive.directive';

describe('AsnOneDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AsnOneDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

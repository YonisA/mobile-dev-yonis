import { AsnOnePipePipe } from './asn-one-pipe.pipe';

describe('AsnOnePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsnOnePipePipe();
    expect(pipe).toBeTruthy();
  });
});
